-r requirements.txt
pytest==9.1.1
httpx==0.28.1

# CI tooling (see .github/workflows/ci.yml) -- not needed to run the app,
# only to lint/typecheck/test it.
ruff==0.16.3
mypy==2.3.1
pytest-cov==7.1.0
pytest-timeout==2.4.0
pytest-rerunfailures==16.6
